# Git Workflow

- Ветка `main` защищена — не коммитить напрямую
- Фича-ветки: `feature/описание`
- Коммиты: осмысленные сообщения, не "fix" или "update"
- ВСЕГДА: typecheck + lint + тесты модуля перед коммитом
- PR: описание что изменилось, кого затрагивает, как откатить
